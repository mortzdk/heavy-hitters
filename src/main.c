#include <stdlib.h>

#include "sketch/sketch.h"

int main (int argc, char **argv) {
	(void) argc;
	(void) argv;

	//sketch_t *sketch = sketch_create(0.2, 0.1);

	//sketch_destroy(sketch);

	return EXIT_SUCCESS;
}
